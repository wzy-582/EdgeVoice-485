# 阶段计划

## 阶段 0：Keil 最小工程与 BSP LED 验证

目标：建立可编译、可烧录、可运行的最小工程基线。

状态：已完成。Keil ARMCC V5.06 编译 0 errors / 0 warnings，板载 LED 可周期闪烁。

范围：

```text
工程入口
系统时钟
BSP 初始化
LED 验证
裸机主循环
```

边界：

```text
FreeRTOS 未接入编译
RS485 未启用
CAN 未启用
Modbus RTU 未实现
ADC 采集业务未实现
```

## 阶段 1：FreeRTOS 内核接入与系统任务

目标：建立 FreeRTOS 最小运行链路。

范围：

```text
FreeRTOS 内核源码
FreeRTOSConfig.h
系统任务
任务创建入口
Hook 回调
SysTick / PendSV / SVC 接管
```

验收：

```text
工程编译通过
调度器正常启动
系统任务周期运行
裸机与 RTOS 配置路径清晰
```

## 阶段 2：基础采集与数据模型

目标：建立 ADC 采集闭环和内部数据模型。

范围：

```text
ADC 通道配置
定时采样入口
采样缓存
基础滤波接口
数据模型读写接口
采集任务
```

验收：

```text
采样数据可更新
数据模型可读取
采样周期稳定
任务间数据访问边界清晰
```

## 阶段 3：RS485 接收链路与协议前置框架

目标：建立 USART/RS485 接收链路、帧缓存和协议解析入口。

范围：

```text
USART 初始化
RS485 收发控制
接收缓冲区
帧边界识别
协议解析入口
接收任务
```

验收：

```text
串口接收稳定
半双工方向控制正常
协议层可获得完整帧
异常帧不影响后续接收
```

## 阶段 4：Modbus RTU 兼容从机与统一发送链路

目标：实现基础寄存器访问、CRC 校验、异常响应和统一发送链路。

范围：

```text
设备地址
功能码处理
CRC16 校验
寄存器映射
异常响应
发送任务
```

验收：

```text
读寄存器功能正常
写寄存器功能正常
CRC 错误帧被丢弃
异常响应格式正确
业务模块不直接占用 UART 发送接口
```

## 阶段 5：CAN 网关与语音命令接入

目标：建立 CAN 收发链路，并将语音命令接入命令分发层。

范围：

```text
CAN 初始化
CAN 收发队列
路由服务
语音 UART 接收
命令码解析
命令分发
```

验收：

```text
CAN 帧可收发
RS485 与 CAN 数据路由可验证
语音命令可触发内部命令
通信错误可统计
```
